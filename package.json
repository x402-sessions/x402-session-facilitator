{
  "name": "x402-sessions-facilitator",
  "version": "0.1.0",
  "description": "Reference x402 facilitator service implementing the `session` scheme for Stellar (SAC approve + transfer_from). Self-host or use as a template.",
  "license": "MIT",
  "private": true,
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^15.0.1",
    "better-sqlite3": "^11.3.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/express": "^4.17.21",
    "@types/node": "^22",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
